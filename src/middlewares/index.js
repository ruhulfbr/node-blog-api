const validateToken = require("./validateToken");
const validateTokenId = require("./validateTokenId");
const validateCategories = require("./validateCategories");

module.exports = {
    validateToken,
    validateTokenId,
    validateCategories,
};
